services:
  database:
    image: postgres:16
    environment:
      POSTGRES_DB: casino
      POSTGRES_USER: casino
      POSTGRES_PASSWORD: change_me

  redis:
    image: redis:latest

  mcp-server:
    build: ./services/mcp-server
    ports:
      - "3000:3000"
